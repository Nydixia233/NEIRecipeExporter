org.gradle.jvmargs=-Xmx2G
org.gradle.daemon=false
org.gradle.configuration-cache=false

# Auto-deploy targets are configured in build.gradle (DEPLOY_MODS_DIRS).
# gradle.properties is read as ISO-8859-1, which mangles non-ASCII paths.
